package miniApp

import (
	"github.com/gin-gonic/gin"
)

func FileIndex(c *gin.Context) {
	//dic := map[string][]string{}
	//err := filepath.Walk(videoDir, func(path string, info os.FileInfo, err error) error {
	//	if info.IsDir() {
	//			dic[info.Name()] = []string{}
	//	}
	//	return nil
	//})
}
